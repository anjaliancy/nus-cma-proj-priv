# Client Feedback — Error Attribution (Table)

Compiled by tracing `git blame`/`git log` directly against the repo (not from memory).

| # | Client-flagged issue | File / lines | Date introduced | Author | Anjali's code? | Intention (if hers) | Status |
|---|---|---|---|---|---|---|---|
| 1 | Vessel rank/capacity — BBX2/BBX3 inconsistency | `servicegraph.py:1545` (flat per-rank capacity constraint); `data_reader.py:153-156` (Vessel built from flat `cap_nom`); `data_reader.py:637` (`cap_eff` parsed but unused) | Mar–Jul 2025 (root constraint); Dec 19, 2025 (`cap_eff` parsing, commit `d19727d`) | Zhuang Linsheng; Darrell | No | — | Open — `cap_eff`/`cap_reserve` exist in data but never wired into the capacity constraint; same flat formula used for every line of a rank |
| 2 | Rotation duration not a multiple of 7 | `servicegraph.py:1216` (duration = hard equality `7 × selected_week`, always correct); root cause of client's manual number = `output_summary.py:174-183` | Duration equality: long-standing. Root cause: May 21, 2026 (commit `f0985aa5`) | zxhalim15 | No | — | Not a separate bug — symptom of #3. `duration` column itself is always correct |
| 3 | Wait/manoeuvring time = 0 for BMX after adding JPYOK | `output_summary.py:174-183` — static `port_details` no longer matches port count after MCTS edits a line; length mismatch zeroes `mantime`, substitutes `waittime` | May 21, 2026 (commit `f0985aa5`, "Fix MILP output FF diagnostics") | zxhalim15 | No | — | Open, real bug — not stale/misread data |
| 4 | VSA "frozen" lines — schedules still modified | Intentional part: `data_reader.py:723-735` (freeze topology/rank, leave speed free), `servicegraph.py:950-955` (pin rank). Gap found this session: VSA stay times not pinned anywhere (no file — an absence) | Jun 25, 2026 (commit `e866fe33`) | **Anjali** | **Yes** (rank/weeks/topology part) | Locking speed on a frozen VSA line adds a hard `speed × time = distance` equality that conflicted with the line's own distance/port-time data → made the MILP **infeasible**. Partner's exact speed doesn't affect slot capacity (= vessel count × class size, both already locked), so speed was deliberately left free | Stay-time gap open — not a mistake in Anjali's fix, just out of its scope. Likely real source of "VSA schedules modified" |
| 5 | Speed limits not respected — BBX constant 19kt | Base hardcode: `vessel.py:50-51` (`min_speed=10`,`max_speed=18`, same for every rank). Soft-cap penalty (matches client's stated rule, 16.5kt/2× fuel): `servicegraph.py:1188-1194`. 5a bug: `servicegraph.py:1191` `cap_index` calc doesn't divide by 0.5kt step, penalty starts at 13.5kt not 17.0kt | Hardcode: Aug 30, 2025. Soft-cap + 5a bug: Dec 19, 2025 (commit `d19727d`) | Zhuang Linsheng; Darrell | No | — | 5a confirmed bug, fix identified (divide by `speed_step`). 5b (why BBX still rides 19.0kt ceiling despite penalty) still **open/unexplained** — hypothesis: schedule-adherence constraint overrides cost deterrent |
| 6 | Ports added with zero cargo movement (CNCWN→CP2, MYLBU→YCX) | `servicegraph.py:404-420` (`get_feasible_actions` — generates add-port moves from rotation-shape validity only, no demand/cargo awareness) | Feb 24, 2025 (base); Feb 4, 2026 (frozen-line filter) | Zhuang Linsheng; zxhalim15 | No | — | Open. Real fix needs a post-solve check in `mcts.py:181` `add_child` — deferred, bigger change, postponed this session |
| 7 | Cargo flow routing not reported in output | Solver computes it (`solution['demand routes']`); export code (`output_summary.py:186-187`) only writes aggregated `segment_flows`/`cargo_flows`, never per-path routing | Dec 19, 2025 (commit `d19727d`) | Darrell | No | — | Open. Data exists, just needs an additional export — not yet implemented |
| 8 | Column definitions — `vrank_mix`; `capacity` vs `weekly_capacity_teu` | `vrank_mix`: no bug, just needed explaining (`"rank:count;rank:count"`, e.g. `"5:2;6:1"`). Bug: `output_summary.py` — `capacity`/`weekly_capacity_teu` both read the same `weekly_capacity` variable, even though solver separately computes real total (`line_capacity_total_teu`, `servicegraph.py:1782-1837`) | May 21, 2026 (commit `f0985aa5`) | zxhalim15 | No | — | **Fixed 2026-08-11** (this session): `capacity` now reads `line_capacity_total_teu`; `weekly_capacity_teu` unchanged. Verified via mock test (2000.0 vs 1000.0) |

**Headline finding:** none of the client-flagged issues trace back to Anjali's own code.
Her one contribution here (VSA freeze logic, Jun 25 2026) was a deliberate, documented,
correct fix for a real infeasibility problem — the new gap found alongside it (VSA stay
time not pinned) is an adjacent omission, not a mistake within that fix.
