Subject: Follow-up on Full-Scope MCTS Output Review — Findings & Fix Plan (Update)

Hi [Client contact name],

Update on the full-scope MCTS output review — statuses below reflect what's now
fixed and pushed since our last note.

| # | Observation | Root Cause | Plan to fix | Current Status / Action Taken |
|---|---|---|---|---|
| 1 | BBX2/BBX3 vessel capacity looks inconsistent — BBX2's 735 TEU reserved-for-slotters figure isn't reflected, and BBX3's effective capacity doesn't reconcile the same way | Capacity is currently calculated using one flat number per vessel type (rank), not factoring in each line's individually reserved/slotted capacity from the proforma data — so BBX2 and BBX3 are both computed the same simplified way. As for the value of BBX3 - the reason for this value is still being traced. | Wire each line's actual reserved-capacity figure into the capacity calculation so it reflects real, per-line numbers instead of one generic figure per vessel type | **Fixed and pushed.** Verified directly against the source data — BBX2's 735 TEU reserved figure now matches the client's own stated number exactly. `cap_scale`/`cap_reserve` wired into both the capacity constraint and the output reporting numbers. One open assumption, not yet confirmed with the client: if MCTS reassigns a line to a different vessel rank, we still apply that line's *original* proforma cap_scale/cap_reserve rather than scaling it to the new vessel size. |
| 2 | BMX rotation duration (25.77 days) isn't a multiple of 7 | Same root cause as #3 - the underlying duration constraint is always an exact multiple of 7; the figure seen came from manually summing per-port timing values affected by the issue in #3 | Resolved once #3 is fixed | **Resolved along with #3.** The duration constraint itself was never the problem — it was always an exact multiple of 7. The 25.77-day figure was a manual sum of the exported wait/manoeuvring columns, which is now fixed (see #3), so that manual sum will add up correctly going forward. |
| 3 | BMX wait/manoeuvring times show as 0 after JPYOK was added, though proforma shows 197 hours | The exported wait/manoeuvring columns matched each line's ports to the proforma record **by list position**, not by which port it actually was. Once a port gets added, the line has one more port than the proforma list, the positions no longer line up, and the code fell back to a hardcoded 0 for **every** port on the whole line — not just the new one | Match each port to its proforma record by port ID instead of position, and give newly-added ports a real fallback value instead of zero | **Fixed and pushed.** Ports are now matched to their proforma record by port ID (handling repeat calls to the same port correctly too). A newly-added port with no proforma record on that line now gets its own real measured wait/manoeuvring data (available for 56 of the 182 ports) instead of a hardcoded 0. Verified directly: inserting JPYOK into BMX now shows its real values (1.48h wait, 5.65h manoeuvring, matching the source data exactly), while every original port on the line keeps its own original number unchanged. |
| 4 | VSA (partner) service schedules appear to be changing even though those lines should be frozen | Topology, weekly schedule, and vessel type are correctly locked for VSA lines. Sailing speed is intentionally left flexible (locking it causes solver infeasibility against the partner's own schedule data). Port stay times, however, are not yet locked the same way - likely source of the drift observed | Lock port stay times for VSA lines, consistent with how topology/schedule/vessel type are already locked | Confirmed topology/schedule/vessel type locking is working as intended. Direction refined: agreed to implement this as a **soft/penalized lock**, not a hard equality — a hard lock risks the same infeasibility failure mode already seen with the speed lock, since distance/time constraints elsewhere can contradict a hard-pinned stay time. **Still not implemented.**<br><br>*Check if these constraints are explicitly mentioned in the proforma. And see which is essential — Meixi guesses that speed may be more important — check the previous docx.* |
| 5 | BBX is sailing at a constant 19kts, appearing to exceed the 16.5kt soft-cap speed rule | The 16.5kt soft-cap penalty does exist in the model, but there is a calibration error in how it's applied - it currently starts discouraging speed too early (13.5kt) rather than at the intended 16.5kt threshold. Why BBX specifically still reaches the 19.0kt ceiling despite the fuel penalty is still under investigation | (a) Fix the miscalibrated penalty so it applies at the correct 16.5kt threshold; (b) continue investigating why BBX rides the top of the allowed range | (a) **Fixed and pushed.** Penalty now correctly starts at 17.0kt (first fleet speed level above the 16.5kt threshold), matching the intended rule. Extracted into a single dedicated function so there's one implementation, and the test suite was rewritten — it was previously checking a fabricated speed grid that never exercised the real code, so it couldn't have caught this bug. All tests now pass against the real fleet data. (b) **Narrowed, still open.** Tested BBX in isolation (its own line, its own local demand, no other lines connected) - the solver picks a modest 11.6kt on its own, not 19kt, and it makes no difference whether the schedule-tethering constraint is on or off. So BBX's own line-level economics don't explain the 19kt behavior; it's very likely a network-wide interaction (e.g. extra cargo transshipping through BBX from other lines) that only shows up once BBX is solved together with the rest of the network. Tried extending the test to a small 3-line connected network to confirm this, but it hit a separate solver-level numerical error (unrelated to the speed question itself) before producing a result. Not yet resolved. |
| 6 | Ports with no cargo movement (CNCWN, MYLBU) are being added to services with no apparent benefit | The model currently doesn't check whether a candidate port addition will actually move any cargo before accepting it into a route - it only checks that the route shape itself is valid | Add a check so the model rejects a port addition if it results in zero cargo movement | **Fixed and pushed.** This one needed a real change inside the core route-search logic, not just an export fix — whether a port moves cargo can only be known after the network is actually solved with it added, not by inspecting the route shape beforehand. Added a check right after a candidate addition is solved: if the solver routes essentially zero cargo through the new port, the candidate is rejected outright and MCTS is blocked from proposing that exact addition again (the same treatment an invalid route shape already gets). Verified directly against real port/line data that the check correctly identifies the added port, correctly distinguishes zero vs. nonzero cargo cases, and correctly blocks the rejected move from being re-offered. A full end-to-end MCTS run to confirm this in practice wasn't possible this round — a reduced-network test hit a separate, pre-existing performance issue (see below) — but every piece of logic the fix adds has been directly verified. |
| 7 | Cargo flow routing isn't visible in the output, making it hard to fully review output quality | The underlying routing is already calculated internally - it's just not currently included in the exported output file | Add cargo flow routing (by OD pair/path) to the exported output | **Implemented and pushed.** New `cargo_flow_routes` sheet added to the output workbook. Verified with a reduced-scope real-data solve that routes with nonzero flow populate correctly. A full full-network end-to-end verification is still pending — unrelated to this fix, blocked by a separate full-network solve performance issue we're tracking separately. |
| 8 | Clarification requested on "vrank_mix", and the difference between "capacity" and "weekly_capacity_teu" | vrank_mix shows the mix of vessel types on a line when more than one type is used (e.g. "5:2;6:1" = 2 ships of type 5, 1 of type 6). Separately, capacity and weekly_capacity_teu were incorrectly showing identical values due to an export bug | vrank_mix: no change needed, documented for clarity. capacity/weekly_capacity_teu: fixed - capacity now correctly shows total fleet capacity, distinct from the per-week figure | Complete. vrank_mix clarified (no change needed). capacity/weekly_capacity_teu duplicate-column bug fixed and verified. |

Best,
[Your name]
